class ArticleModel {

  String description;
  String title;
  String img;

  //Now let's create the constructor
  ArticleModel(
      {
        required this.description,
        required this.title,
        required this.img,
       });


}